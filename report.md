keyboard test bug report

1. ปุ่ม test ไม่ทำงานเมื่อกด กลายเป็นกระโดด jump to button (reset/done)
2.r control กดแล้วไม่ขึ้นเขียว

3. r alt กดแล้วไม่ขึ้นเขียว

4. windows key กดแล้วไม่ขึ้นเขียว
5. ปุ่มตัวหนอน กดแล้วไม่ขึ้นเขียว

6. numpad lock กดแล้วไม่ขึ้นเขียว
7. numpad / กดแล้วไม่ขึ้นเขียว
8. numpad ent กดแล้วไม่ขึ้นเขียว
9. ins home del end pgup pgdn กดแล้วไม่ขึ้นเขียว
10 . ปุ่มลูกศร กดแล้วไม่ขึ้นเขียว 